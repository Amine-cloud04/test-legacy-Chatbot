# Minimal dependencies for the USB offline bundle (Windows, no web on company PC).
# Excludes optional vector/GGUF stacks that are hard to vendor offline.
pdfminer.six==20221105
python-docx==1.1.2
python-pptx==0.6.23
rank-bm25==0.2.2
office365-rest-python-client==2.5.11
streamlit==1.35.0
tqdm==4.66.4
python-dotenv==1.0.1
httpx==0.28.1
